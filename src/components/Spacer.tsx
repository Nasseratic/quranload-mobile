import { View } from "react-native";

export const XSpacer = ({ space }: { space: number }) => <View style={{ width: space }} />;

export const YSpacer = ({ space }: { space: number }) => <View style={{ height: space }} />;
