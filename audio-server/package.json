{
  "name": "audio-server",
  "version": "1.0.0",
  "description": "Bun server for audio chunk concatenation and upload",
  "main": "server.ts",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "convex": "^1.31.2",
    "dotenv": "^17.2.3",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5"
  }
}
